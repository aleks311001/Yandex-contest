#include <iostream>
#include <vector>
#include <string>

class BigInteger {
public:
	std::string toString() {
		std::string t = NULL;
		for (int i = 0; i < n.size(); i++)
			t = t + static_cast <int> 
	}
private:
	std::vector<int> n;

};

int main()
{
    return 0;
}
